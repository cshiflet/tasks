[package]
name = "tasks-ui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Qt 6 (cxx-qt) front-end for the Tasks.org native desktop client. Not yet wired up — see README for build status."

[[bin]]
name = "tasks-desktop"
path = "src/main.rs"

[dependencies]
tasks-core = { path = "../tasks-core" }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
# cxx-qt integration intentionally deferred — see desktop-native/README.md.
# cxx-qt = "0.6"
# cxx-qt-lib = "0.6"
